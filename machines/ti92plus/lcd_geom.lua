-- TI-92 Plus physical LCD geometry only (no controller / no bit ops).
-- Safe for thin CC clients to require without pulling the emu.

return {
  WIDTH = 240,
  HEIGHT = 128,
  BYTES_PER_ROW = 30,
  LOGICAL_WIDTH = 240,
  LOGICAL_HEIGHT = 128,
}
